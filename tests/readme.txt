Just a little test project.

Run either build.bat, or build.py to build it. Both do the same thing but in
different ways.

build.py requires Python to use. Python's pretty good, I recommend it. And
it's basically a one-click install for Windows, no cygwin or anything.

